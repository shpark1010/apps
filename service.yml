apiVersion: v1
kind: Service
metadata:
  name: alb-dev-service
  namespace: alb-dev-app
  labels:
    app: httpd
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP 
  selector:
    app: httpd